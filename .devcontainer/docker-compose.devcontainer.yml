services:
  devcontainer:
    build:
      context: ..
      target: deps
    command: sleep infinity
    volumes:
      - ..:/build
      - devcontainer-node-modules:/build/node_modules
      - devcontainer-web-modules:/build/web/node_modules
      - ${WORKSPACE_PATH:-../newworkspace}:/data
    environment:
      - WORKSPACE=/data
      - NODE_ENV=development

volumes:
  devcontainer-node-modules:
  devcontainer-web-modules:
