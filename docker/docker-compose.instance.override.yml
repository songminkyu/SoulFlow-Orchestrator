# 멀티 인스턴스 오버라이드: redis/docker-proxy 비활성화, 기존 네트워크 참여
services:
  docker-proxy:
    profiles: ["disabled"]
  redis:
    profiles: ["disabled"]

networks:
  default:
    external: true
    name: soulflow-${BASE_PROFILE}-network
